dist: trusty
language: node_js
node_js:
    - '11'
branches:
    only:
        - master
addons:
    sonarcloud:
        organization: 'flowscripter'
        token:
            secure: $SONAR_TOKEN
script:
    - npm run coverage
    - sonar-scanner
